[user]
[push]
  default = matching
[alias]
  co = checkout
  br = branch
  ci = commit
  st = status
  who = shortlog -s --
  last = log -1 HEAD
  unstage = reset HEAD --
  graph = log --graph --abbrev-commit --oneline
  amend = commit --amend --no-edit
